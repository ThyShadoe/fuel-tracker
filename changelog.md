# CHANGELOG

## 2025-11-14

### What's New

- Added functionality to edit existing entries.
- Added simple regex validation for date format.

### Changes / Fixes

- Updated validation for choices in `Manage Entries` menu.
- Updated variable names in some setter functions to match other variables.
- Removed delay and warning when entering incorrect choices in menus. Instead,
  it instantly resets.
- Removed `TODO` for date validation and editing entries.
- Removed `checks.md` as it was no longer needed.
- Removed dupe `#include <iostream>` in `main.cpp`.
